"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const path_1 = require("path");
function mkdirsSync(dir) {
    if (fs_1.existsSync(dir)) {
        return true;
    }
    else {
        if (mkdirsSync(path_1.dirname(dir))) {
            fs_1.mkdirSync(dir);
            return true;
        }
    }
}
exports.mkdirsSync = mkdirsSync;
function getWorkspaceConfig() {
    return (fs_1.existsSync('./autojs.json') && JSON.parse(fs_1.readFileSync('./autojs.json', 'utf-8'))) || null;
}
exports.getWorkspaceConfig = getWorkspaceConfig;
function firstUpperCase(str) {
    return str.split('').map((val, index) => {
        return index === 0 ? val.toUpperCase() : val;
    }).join('');
}
exports.firstUpperCase = firstUpperCase;
