"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const program = require("commander");
const adb_1 = require("./adb");
const util_1 = require("./util");
program
    .option('-d, --dir <dirname>', '保存目录', process.cwd())
    .parse(process.argv);
const config = util_1.getWorkspaceConfig();
const saveDir = (config && (config.screencap && config.screencap.dir)) || program.opts().dir;
console.log('请确认好已安装adb并且在Path中设置可全局调用！');
console.log('请确认您的计算机已被设备允许USB调试！');
try {
    adb_1.ScreenCap((program.args[0] && `${program.args[0]}.png`) || `${Date.now()}.png`, saveDir);
}
catch (error) {
    console.log(error);
}
